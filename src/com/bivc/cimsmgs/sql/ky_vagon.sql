SELECT A.NVAG, B.DPRB, CASE WHEN A.PORUZ = 1 THEN 'P' ELSE 'L' END AS PORUZ, A.SOBSTV, A.REVIZ, A.PROBEG, A.DOTP, A.PRIM FROM KY_VAGON A
  JOIN KY_POEZD B ON B.DIRECTION=1 AND A.HID_POEZD=B.HID AND B.DPRB>=? AND B.DPRB<?+1 WHERE A.FOOT=?