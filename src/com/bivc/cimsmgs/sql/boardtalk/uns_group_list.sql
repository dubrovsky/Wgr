SELECT DISTINCT u.GROUP_ID, u.GROUP_ID AS text, TRUE AS expanded, FALSE AS checked FROM USR u
WHERE u.GROUP_ID IN (%s) OR EXISTS (SELECT g.USR_ID FROM USR_GROUPS g WHERE u.UN = g.USR_ID AND g.GROUP_ID IN (%s))