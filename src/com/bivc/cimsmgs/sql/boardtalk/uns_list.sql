SELECT u.UN, IFNULL(u.NAM_KLIENT,u.UN) AS text, TRUE AS leaf, FALSE AS checked FROM USR u
WHERE (u.GROUP_ID IN (%s) OR EXISTS (SELECT g.USR_ID FROM USR_GROUPS g WHERE u.UN = g.USR_ID AND g.GROUP_ID IN (%s))) AND u.GROUP_ID=?